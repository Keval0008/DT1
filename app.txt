result = deepcopy(pbt_frp)

for key in opening_pbt:
    if key in result and result[key]:  # Check if key exists and dict isn't empty
        min_year = min(result[key].keys())  # Find the minimum year dynamically
        result[key][min_year] -= opening_pbt[key]  # Subtract from the earliest year
